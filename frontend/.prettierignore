# Dependencies & the pnpm virtual store
node_modules
.pnpm-store

# Python virtualenvs & test / type-check caches (NEVER format)
.venv
__pycache__
.pytest_cache
.mypy_cache
.ruff_cache

# Lockfiles (managed by pnpm — never reformat by hand)
pnpm-lock.yaml

# Build / cache output (regenerated by tsup / turbo)
dist
.turbo

# Version control
.git

# Files generated by the setup CLI / build (do NOT hand-format)
*.generated.ts
modules.paths.json
tsconfig.paths.json

# Vendored / minified third-party assets
*.min.js
*.min.css
